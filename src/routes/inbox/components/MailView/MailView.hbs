<div class="mail-view">
    <div class="mail-view__toolbar">
        <button class="mv-btn" data-action="back" title="Назад">←</button>
        <button class="mv-btn" data-action="delete" title="Удалить">🗑</button>
        <button class="mv-btn" data-action="move" title="В папку">📁</button>
        <button class="mv-btn" data-action="spam" title="Спам">🚫</button>
        <button class="mv-btn" data-action="reply" title="Ответить">↩</button>
        <button class="mv-btn" data-action="forward" title="Переслать">↪</button>
    </div>

    <div class="mail-view__header">
        <h1 class="mail-view__subject">{{subject}}</h1>
        <div class="mail-view__meta">
            <div class="mail-view__avatar">{{initials}}</div>
            <div class="mail-view__from">
                <div class="mail-view__from-name">{{from}}</div>
                <div class="mail-view__time">{{time}}</div>
            </div>
        </div>
    </div>

    <div class="mail-view__body">{{{body}}}</div>
</div>
